{
  "name": "rbcp3-auth-styles",
  "version": "1.0.1",
  "scripts": {
    "build": "npm run version",
    "watch": "./bin/watch",
    "version": "./bin/version",
    "postversion": "./bin/postversion"
  },
  "devDependencies": {
    "less": "^2.7.1",
    "less-plugin-clean-css": "^1.5.1",
    "onchange": "^2.5.0"
  }
}
